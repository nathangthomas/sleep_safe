require 'rails_helper'

describe 'user can see nav bar' do
  describe 'as a user when I visit the welcome page I see a nav bar on the top of the page' do
    describe 'it has a field to search for shelters by zipcode and '


In the nav bar on the left side I see a search feature with two fields (zip code and radius) that searches for shelters by zip code and allows for an optional radius.

I also see a search button
